{
  "name": "nerdr",
  "private": true,
  "version": "0.1.0",
  "description": "Herdr-like agent awareness for OpenCode agents running inside VSCodium",
  "license": "MIT",
  "workspaces": [
    "packages/protocol",
    "packages/opencode-plugin",
    "packages/extension"
  ],
  "scripts": {
    "build": "npm run build -w @nerdr/protocol && npm run build -w @nerdr/opencode-plugin && npm run build -w nerdr",
    "typecheck": "npm run typecheck -w @nerdr/protocol && npm run typecheck -w @nerdr/opencode-plugin && npm run typecheck -w nerdr",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules",
    "package": "npm run build && npm run package -w nerdr"
  }
}
